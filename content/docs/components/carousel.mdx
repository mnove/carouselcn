---
title: Carousel
description: A lightweight carousel component with CSS transitions and drag support. No additional animation library required.
---

import { TypeTable } from 'fumadocs-ui/components/type-table';

<ComponentPreview name="carousel-demo" />

## Installation

<CodeBlockCommand component="carousel" />

---

## Usage

```tsx
import {
  Carousel,
  CarouselContent,
  CarouselItem,
  CarouselNavigation,
  CarouselIndicator,
} from "@/components/ui/carousel";
```

```tsx
<Carousel>
  <CarouselContent>
    <CarouselItem>Slide 1</CarouselItem>
    <CarouselItem>Slide 2</CarouselItem>
    <CarouselItem>Slide 3</CarouselItem>
  </CarouselContent>
  <CarouselNavigation />
  <CarouselIndicator />
</Carousel>
```

---

## Examples

### Basic

<ComponentPreview name="carousel-demo" />

### Custom Indicator

Use controlled state with `index` and `onIndexChange` to build custom indicators.

<ComponentPreview name="carousel-custom-indicator-demo" />

### Loop

Enable looping with the `loop` prop. When reaching the last slide, navigating forward jumps back to the first slide.

<ComponentPreview name="carousel-loop-demo" />

### Sizes and Spacing

Use `-ml-4` on `CarouselContent` and `pl-4` on `CarouselItem` to add spacing between items. Use `basis-1/3` (or other values) to show multiple items at once.

<ComponentPreview name="carousel-spacing-demo" />

---

## API Reference

### Carousel

The root component that provides context for the carousel.

<TypeTable
  type={{
    initialIndex: {
      description: 'The initial slide index',
      type: 'number',
      default: '0',
    },
    index: {
      description: 'Controlled slide index',
      type: 'number',
    },
    onIndexChange: {
      description: 'Callback when slide changes',
      type: '(index: number) => void',
    },
    disableDrag: {
      description: 'Disable drag-to-navigate',
      type: 'boolean',
      default: 'false',
    },
    loop: {
      description: 'Enable looping (jumps back to start)',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
    },
  }}
/>

### CarouselContent

Container for carousel items with drag and transition support.

<TypeTable
  type={{
    transition: {
      description: 'CSS transition options',
      type: '{ duration?: number; ease?: string }',
      default: '{ duration: 300, ease: "ease-out" }',
    },
    className: {
      description: 'Additional CSS classes',
      type: 'string',
    },
  }}
/>

### CarouselItem

Individual slide wrapper.

<TypeTable
  type={{
    className: {
      description: 'Additional CSS classes',
      type: 'string',
    },
  }}
/>

### CarouselNavigation

Previous/next navigation buttons.

<TypeTable
  type={{
    alwaysShow: {
      description: 'Always show navigation (vs. hover)',
      type: 'boolean',
      default: 'false',
    },
    className: {
      description: 'Container CSS classes',
      type: 'string',
    },
    classNameButton: {
      description: 'Button CSS classes',
      type: 'string',
    },
  }}
/>

### CarouselIndicator

Dot indicators showing current slide position.

<TypeTable
  type={{
    className: {
      description: 'Container CSS classes',
      type: 'string',
    },
    classNameButton: {
      description: 'Dot button CSS classes',
      type: 'string',
    },
  }}
/>

### useCarousel

Hook to access carousel state and controls.

```tsx
const { index, setIndex, itemsCount } = useCarousel();
```

---

## Comparison with Carousel Motion

This component uses CSS transitions instead of Motion (Framer Motion). Choose this version if:

- You want a smaller bundle size
- You don't need spring-based animations
- You're not already using Motion in your project

For smoother, spring-based animations, consider using [Carousel Motion](/docs/components/carousel-motion).
